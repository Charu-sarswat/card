import React from "react";

function Sidebar({ filters, setFilters }) {
    return (
        <div className="sidebar">
            {/* Sidebar is currently empty */}
        </div>
    );
}

export default Sidebar; 